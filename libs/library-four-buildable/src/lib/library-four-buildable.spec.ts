import { libraryFourBuildable } from './library-four-buildable';

describe('libraryFourBuildable', () => {
  it('should work', () => {
    expect(libraryFourBuildable()).toEqual('library-four-buildable');
  });
});
